import { Box } from "@mui/material";
import React from "react";

function Footer(props) {
  return (
    //Renderizado diferente para cada componente, condicional
    <footer>
      <Box></Box>
    </footer>
  );
}

export default Footer;
